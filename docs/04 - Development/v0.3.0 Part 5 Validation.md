# v0.3.0 Part 5 Validation

> Status: Historical v0.3.0 validation record

## Automated checks completed

- JavaScript syntax validation for `core`, `modules`, `manifest`, and `tests`
- Cache manager unit test
- Event bus diagnostics unit test
- Existing event bus unit test
- Existing service and object registry unit tests
- Existing formation, unit, battle-object, and compatibility unit tests
- Chrome extension build

## Live checks still required

- Load the unpacked `dist/chrome` extension
- Confirm game discovery and compatibility detection
- Confirm current player, city, world, alliance, and selected-base values
- Enter and leave combat to confirm battle events
- Switch cities and selections to confirm cache invalidation
- Leave the game idle and confirm watchdog remains healthy
- Inspect `CnCTASuite.diagnostics.snapshot()` in the browser console

These were the live acceptance requirements for v0.3.0. Current releases follow `Releases.md` and the current integration checklists.
